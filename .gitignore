# Regenerable source HTML dumps (re-fetched every run) — not committed.
scraper/fetched/

# Intermediate pipeline JSON (regenerated by pipeline.py)
scraper/merged.json
scraper/eca_all.json

# OS / editor cruft
.DS_Store
*.swp
